// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';

// Fallback values in case env vars aren't loaded
const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || 'https://drvitjhhggcywuepyncx.supabase.co';
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || 'your_fallback_anon_key';

if (!SUPABASE_URL || !SUPABASE_ANON_KEY) {
  console.error('Supabase environment variables are missing');
}

export const supabase = createClient(
  SUPABASE_URL,
  SUPABASE_ANON_KEY,
  {
    auth: {
      persistSession: true,
      autoRefreshToken: true,
      detectSessionInUrl: true
    },
    global: {
      headers: {
        'Accept': 'application/json',
        'Content-Type': 'application/json',
        'apikey': SUPABASE_ANON_KEY
      }
    }
  }
);